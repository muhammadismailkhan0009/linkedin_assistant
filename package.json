{
    "scripts": {
        "start": "parcel watch manifest.json src/content.js --host localhost --config @parcel/config-webextension",
        "build": "parcel build manifest.json src/content.js --config @parcel/config-webextension"
    },
    "dependencies": {
        "@parcel/config-webextension": "^2.15.2"
    }
}