{
    "scripts": {
        "start": "rm -rf dist/* && rm -rf .parcel-cache/* && parcel watch manifest.json src/background/content.js --host localhost --config @parcel/config-webextension",
        "build": "rm -rf dist/* && rm -rf .parcel-cache/* && parcel build manifest.json src/background/content.js --config @parcel/config-webextension"
    },
    "dependencies": {
        "@parcel/config-webextension": "^2.15.2",
        "@types/react": "^19.1.7",
        "@types/react-dom": "^19.1.6",
        "parcel": "^2.15.4",
        "react": "^19.1.0",
        "react-dom": "^19.1.0"
    }
}