{
    "scripts": {
        "start": "rm -rf dist/* && parcel watch manifest.json src/backgroundcontent.js --host localhost --config @parcel/config-webextension",
        "build": "rm -rf dist/* && parcel build manifest.json src/background/content.js --config @parcel/config-webextension"
    },
    "dependencies": {
        "@parcel/config-webextension": "^2.15.2",
        "@types/react": "^19.1.7",
        "@types/react-dom": "^19.1.6"
    }
}
